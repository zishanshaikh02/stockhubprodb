from django.apps import AppConfig


from django.apps import AppConfig

class ImagesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'images'  # Add this line to set the app_label
